# Ejemplo de configuración de variables de entorno
# Copiar este archivo a .env y configurar los valores

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=your-account-id-here

# Project Configuration
PROJECT_NAME=ProductsApp
ENVIRONMENT=prod

# EC2 Configuration
EC2_KEY_NAME=my-ec2-key
EC2_INSTANCE_TYPE=t3.small

# API Gateway URL (se obtiene después del despliegue)
API_GATEWAY_URL=https://your-api-gateway-url.execute-api.us-east-1.amazonaws.com/prod

# DynamoDB Configuration
DYNAMODB_TABLE=ProductsApp-prod-Products

# ECR Repositories (se crean automáticamente)
LAMBDA_ECR_REPO=ProductsApp-lambda-prod
EC2_ECR_REPO=ProductsApp-ec2app-prod
